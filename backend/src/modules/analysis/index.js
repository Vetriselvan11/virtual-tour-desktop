const routes = require('./analysis.routes');
const service = require('./sceneAnalysis.service');
const controller = require('./analysis.controller');

module.exports = {
  routes,
  service,
  controller
};
